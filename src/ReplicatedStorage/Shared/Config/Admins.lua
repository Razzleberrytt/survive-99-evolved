return {
  Allowlist = {
    -- e.g., 12345678,
  },
  Group = {
    GroupId = 0,   -- set to your group id or leave 0 to disable group check
    MinRank = 255, -- minimum rank allowed; ignored if GroupId == 0
  },
}
