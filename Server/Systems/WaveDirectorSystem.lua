local GameConfig = require(game.ReplicatedStorage.Shared.Config.Game)

return function(world)
    -- Compute per-night budget and schedule micro-waves using GameConfig.Wave
    -- TODO: integrate with matter world and spawn points
end
